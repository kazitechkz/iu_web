export enum AccountActionTypes {
    OnAccount = "[Account Action] On Account",
    OnAccountSuccess = "[Account Action] On Account Success",
    OnAccountFailure = "[Account Action] On Account Failure",
}

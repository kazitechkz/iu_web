export enum PassSubStepActionTypes {
    OnPassSubStep = "[On Pass SubStep Action] On Pass SubStep Action",
    OnPassSubStepClear = "[On Pass SubStep Clear Action] On Pass SubStep Clear Action",
    OnPassSubStepSuccess = "[On Pass SubStep Action] On Pass SubStep Action Success",
    OnPassSubStepFailure = "[On Pass SubStep Action] On Pass SubStep Action Failure",
}

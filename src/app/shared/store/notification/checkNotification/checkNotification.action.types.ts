export enum CheckNotificationActionTypes {
  CheckNotificationAction = "[On Check Notification Action] On Check Notification",
  CheckNotificationActionSuccess = "[On Check Notification Action] On Check Notification Success",
  CheckNotificationActionFailure = "[On Check Notification Action] On Check Notification Failure",
}

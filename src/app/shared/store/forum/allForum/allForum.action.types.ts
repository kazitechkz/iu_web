export enum AllForumActionTypes {
  OnAllForum = "[On All Forum Action] On All Forum Action",
  OnAllForumSuccess = "[On All Forum Action] On All Forum Action Success",
  OnAllForumFailure = "[On All Forum Action] On All Forum Action Failure",
}

export enum SaveQuestionActionTypes {
  SaveQuestion = "[Save Question Action] Save Question Action",
  SaveQuestionSuccess = "[Save Question Action] Save Question Action Success",
  SaveQuestionFailure = "[OSave Question Action] Save Question Action Failure",
}

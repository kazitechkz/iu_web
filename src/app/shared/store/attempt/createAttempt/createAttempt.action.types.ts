export enum CreateAttemptActionTypes {
  OnCreateAttempt = "[Create Attempt Action] On Create Attempt",
  OnCreateAttemptSuccess = "[Create Attempt Action] On Create Attempt Success",
  OnCreateAttemptFailure = "[Create Attempt Action] On Create Attempt Failure",
}

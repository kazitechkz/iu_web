export enum PayCreateActionTypes {
  OnPayCreate = "[On Pay Create Action] On Pay Create",
  OnPayCreateSuccess = "[On Pay Create Action] On Pay Create Success",
  OnPayCreateFailure = "[On Pay Create Action] On Pay Create Failure",
}

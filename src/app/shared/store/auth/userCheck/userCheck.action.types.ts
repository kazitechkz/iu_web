export enum UserCheckActionTypes {
    OnUserCheck = "[On User Check] On Account",
    OnUserCheckSuccess = "[On User Check] On Account Success",
    OnUserCheckFailure = "[On User Check] On Account Failure",
}
